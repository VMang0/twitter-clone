import { LoaderContainer } from '@components/Loader/styled';
import { Loader as LoaderIcon } from '@styled/components/loader/styled';

export const Loader = () => (
  <LoaderContainer>
    <LoaderIcon color="#1e97e1" />
  </LoaderContainer>
);
